# 导航系统端口配置文件
# 此文件定义了系统中所有固定端口，避免随意变动
# DO_NOT_MODIFY_WHEN_GENERATING_CODE_BEGIN

# 前端服务端口配置
FRONTEND_PORT=8080
FRONTEND_INTERNAL_PORT=8000

# 后端API服务端口配置
BACKEND_PORT=5002
BACKEND_INTERNAL_PORT=5001

# Nginx反向代理端口（生产环境）
NGINX_PORT=80

# 端口说明
# FRONTEND_PORT: 前端服务对外暴露的端口
# FRONTEND_INTERNAL_PORT: 前端服务在容器内部使用的端口
# BACKEND_PORT: 后端API服务对外暴露的端口
# BACKEND_INTERNAL_PORT: 后端API服务在容器内部使用的端口

# 注意事项
# - 8080: 前端页面访问端口，映射到Docker容器内部的8000端口
# - 5002: 后端API服务端口，映射到Docker容器内部的5001端口
# - 80: Nginx反向代理端口（仅生产环境使用）
# DO_NOT_MODIFY_WHEN_GENERATING_CODE_END